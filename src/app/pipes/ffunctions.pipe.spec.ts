import { FfunctionsPipe } from './ffunctions.pipe';

describe('FfunctionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FfunctionsPipe();
    expect(pipe).toBeTruthy();
  });
});
