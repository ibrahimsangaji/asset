import { FassetstatusPipe } from './fassetstatus.pipe';

describe('FassetstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FassetstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
